"
I'm the trivial resolution so I expect collection names tp be the same as the Mapless class names
"
Class {
	#name : #MaplessTrivialResolver,
	#superclass : #MaplessResolver,
	#category : #'Mapless-Base-Core-Resolvers'
}
