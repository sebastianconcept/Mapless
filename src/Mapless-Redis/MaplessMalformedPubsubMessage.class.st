"
Mapless observer messaging have an expected payload and when there is one that hasn't it right I can be signaled
"
Class {
	#name : #MaplessMalformedPubsubMessage,
	#superclass : #MaplessError,
	#category : #'Mapless-Redis-Core'
}
