#!/bin/bash
#
# Cleans up libraries and modules that can be installed from remote sources and
# local files that can be rebuilt from sources

rm -rf bower_components/
rm -rf npm_modules/
rm -f src/*.js
rm -f the.js
rm -f config.js